# PumpLog release tracker

| Version | Date/Timestamp (published) | Branch | Live App | Summary of changes |
|---|---|---|---|---|
| v1.7.0 | 2026-07-30 | `arena/019fb152-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | **Fuel stock tracking (Phase 5 MVP) + Dashboard polish.** New `js/stock.js` persists per-product tank levels (`stations/{id}/stock/current`) and an append-only stock log (`stations/{id}/stockLog`) with four entry types: delivery (add litres), dip (set exact level from the stick), manual adjustment, and test measure. Config → Fuel stock section shows current levels with low/empty colour coding and a "Manage stock" modal for managers (entry form + recent entries). The Dashboard gets a matching stock card visible to every signed-in station member (managers see the Manage button). Team search now matches phone numbers and employee IDs; the user-created dialog correctly shows phone instead of email for phone-only accounts and formats the copy-credentials text accordingly. Firestore rules add read/write for the new stock documents (station-member reads, manager writes with field validation). **Requires re-publishing `firestore.rules`.** |
| v1.6.0 | 2026-07-30 | `arena/019fb152-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | **PIN reset flows + phone-login hardening.** Manager "Reset PIN" now uses Firebase Auth's password-reset email (sends a one-time link to the staff member's real email); they land on a new `set-pin.html` page and pick their own 4–8 digit Cloud PIN directly. The sign-in screen has a matching "Forgot Cloud PIN?" self-service link (hidden when phone-only mode is on). Phone + PIN sign-in now authenticates straight against the synthetic `phone:<digits>@pumplog.local` Auth email created at account-creation time, removing the pre-auth Firestore `phoneNumber` lookup that Firestore rules would have denied and that leaked the phone→uid mapping. Removed the unreachable `pin_temporary` Firestore field and its sign-in fallback (the client SDK can never rotate another user's Firebase Auth password on Spark plan). The Add/Edit Team forms now mark email as recommended (required for self-serve PIN reset), document the "deactivate + recreate" limitation for login-method changes, and Team Board/staff pickers filter out disabled and invited accounts so deactivated staff no longer appear in rosters or tallies. Profile → Security wording mentions phone alongside email; DEPLOYMENT.md rewritten to cover the reset flow, phone identity model, and Firebase Auth "Authorized domains" requirement. **Requires re-publishing `firestore.rules`.** |
| v1.5.0 | *(rolled into v1.6.0)* | `arena/019fb152-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Phone + Cloud PIN groundwork (per-station toggle, E.164-ish phone validation, UI tabs, phone shown in Profile and Team cards). Superseded by v1.6.0. |
| v1.4.0 | 2026-07-30 | `arena/019fb152-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | **Team Board redesign + Dashboard stats strip.** Removed the floating refresh FAB that overlapped content (the top-bar Refresh button remains) and removed all dead `.fab*` CSS rules plus the stale selector from the print stylesheet. Renamed the "Who's where" tab to **Team Board** consistently across the tab label, page `<h1>`, tooltips, empty states, and README. Redesigned Team Board from nested card-in-a-card pump cards to a flat, phone-first list: one row per pump with columns for a status dot (● Active / ○ Idle / ✕ Unassigned), pump name/product, assignee name inline, status label, elapsed time, and today's volume/sales totals fed by a live `watchShifts` subscription. Tap-to-assign, date navigation, copy/clear, and the Assign dialog still work the same. Collapsed the Dashboard's 4 separate "MY TODAY" + "STATION" bordered cards into one borderless stats strip (Rates section unchanged). Summary counters ("X of Y pumps covered · X of Y staff rostered") now filter staff to those actually attached to the station, fixing the reported "1 of 0 staff rostered" glitch. Responsive layouts tested at 375px and 428px. No Firestore rules changes. |
| v1.3.0 | 2026-07-28 | `arena/019fa99c-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | **Fixes the staff "not authorized to start shift" lockout** and replaces one-off pump assignment with a daily **Roster board** (Kanban/Trello style). Root cause: `canUsePump()` treated an empty `pumpIds` list as "no pumps at all", so every staff account was blocked from Start/End shift until an admin hand-edited their profile — an empty list now means *unrestricted* again, matching `firestore.rules`. New `stations/{id}/assignments/{date}_{pumpId}` collection stores the day's roster; new **Roster** tab shows one column per pump plus an "Available" column, with drag-and-drop (and tap-to-move plus an Assign dialog for touch/keyboard), live status per card (on shift + elapsed time, shift ended + volume/sales, last sign-in), date navigation, copy-previous-day, and clear. Rostering is live on every device via snapshot listeners, and the signed-in profile is now watched too, so role/station/pump changes apply without signing out. Also: Managers and Station Admins can now see and roster **all** staff at their stations (previously only accounts they personally created, which hid half the team from the board, Config → Team, and Reports), the Reports tab is no longer hidden from Managers, and the pump-card ⋮ menu is always reachable. **Requires re-publishing `firestore.rules`.** |
| v1.2.0 | 2026-07-28 | `dev/roles-manager-shift-approvals` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Station Manager role (mid-tier between Station Admin and Staff), manager/admin shift start/end controls on Pumps and Dashboard, clock-out form with Notes, Expenses, and Cash-to-hand-over computation, shift approval workflow (pending/approved/rejected) with single and bulk approval from History, status filters on History, pending-amount breakdown on Dashboard and Reports, status column in CSV exports, plus bug fixes: pump session writes now always include pumpName/product (E1), configurable initial reading on pumps (E2), Rates section shows latest per product only (E3). |
| v1.1.0-free | 2026-07-28 | `arena/019fa61d-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Free/Spark-plan testing mode: removed Firebase Cloud Functions deployment and browser callable Functions usage, simplified sign-in to email + Cloud PIN via Firebase Auth, kept Firestore direct writes for app data/profile metadata, allowed users to change their own Cloud PIN from Profile, disabled invite/admin credential reset flows that need Admin SDK, and changed CI/docs to deploy Firestore rules only with no Blaze/pay-as-you-go requirement. |
| v1.0.3 | 2026-07-27 | `arena/019fa56b-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Deploy docs/CI hardening: documented the IAM **Editor** role requirement for the deploy service account (fixes `403 Permission denied to get service` when using a self-created service account), added the exact error→fix mapping to the workflow failure hints and DEPLOYMENT.md troubleshooting table, and warned that Google auto-revokes service-account keys leaked in public repos. |
| v1.0.2 | 2026-07-27 | `arena/019fa56b-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Deploy hotfix: upgraded the Cloud Functions runtime to `nodejs22` (Node 20 is past EOL), and shipped workflow v2 — validates the `FIREBASE_SERVICE_ACCOUNT` secret before deploying (clear errors for malformed/truncated/wrong-project keys), pins `firebase-tools@15`, runs jobs on Node 22, and prints self-service troubleshooting steps on any deploy failure. |
| v1.0.1 | 2026-07-27 | `arena/019fa56b-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Zero-touch deployment: ready-to-install GitHub Actions workflow at `ci/deploy-firebase.yml` that auto-deploys Cloud Functions **and** `firestore.rules` on every push (one-time `FIREBASE_SERVICE_ACCOUNT` secret), pinned the Firebase project via `.firebaserc`, added a root `.gitignore` blocking credential files, and shipped a 3-step go-live guide in `DEPLOYMENT.md`. |
| v1.0.0 | 2026-07-27 | `arena/019fa56b-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Production SaaS release: four station-configurable sign-in methods (email/username × password/Cloud PIN) with per-station security settings (method toggles, App Lock triggers, credential policies, PIN rotation) applied instantly; new device-local App Lock with PIN + security-question reset, never synced; Cloud PIN (now 4–8 digits) changeable from Profile, hashed server-side with rotation enforcement; full admin user CRUD with temporary passwords/Cloud PINs, forced first-login changes, avatars, deactivate/remove; standardized icons, confirm dialogs, and toasts across every CRUD action; developer bootstrap, legacy-migration tools, and self-signup removed. **Requires redeploying `functions` and re-publishing `firestore.rules`.** |
| v0.5.0 | 2026-07-27 | `arena/019fa430-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Fixed usernames to 4–6 characters; added developer bootstrap for UID `gVXWLjIIpXMa26kxdKuZUEGCAwj1`, custom `dev01` + 4-digit PIN login, secure 10-digit Station Admin invites, and reversible tracked activation. |
| v0.4.0 | 2026-07-27 | `arena/019fa430-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Added trusted Firebase Functions username/PIN identity backend and static client flow: server-side scrypt hashing, one-time joining codes, custom-token authentication, lockouts, admin reset/change/disable, audits, legacy-account preparation, staff creation UI, username validation, and profile security. |
| v0.3.0 | 2026-07-27 | `arena/019fa430-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Confirmed the existing atomic staff shift flow and added a live Station Admin/Super Admin pump board with direct assignment/removal through existing `users/{uid}.pumpIds`, active-session warnings without force-ending shifts, and a read-only Profile accordion. Option A retains `stationadmin` as both pump-board and staff manager. |
| v0.2.0 | 2026-07-27 | `arena/019fa430-gaspipe` | [Open PumpLog](https://ranadheerrj.github.io/gaspipe/) | Added transactional pump clock-in/out sessions with live locks, Reports with CSV/print export, station-data reset and force-release controls, overnight-safe hours handling, redesigned Dashboard, and card-based Config accordions. Firestore rules were extended and require manual re-publishing. |
